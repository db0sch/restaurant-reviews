<div class="container">
  <div class="row">
    <div class="col-6 offset-3">
      <h1>Add rreview for <%= @restaurant.name %></h1>
      <%= simple_form_for([@restaurant, @review]) do |f| %>
        <%= f.input :content %>
        <%= f.submit "Submit review" %>
      <% end %>
    </div>
  </div>
</div>

<!-- Review.new
POST /reviews -> reviews_path -->
<!-- POST /restaurants/42/reviews/ -->
